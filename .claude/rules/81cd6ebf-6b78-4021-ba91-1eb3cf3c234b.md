<rule_activation id="81cd6ebf-6b78-4021-ba91-1eb3cf3c234b" title="Adopt MCP Server Pattern for Service Integration Boundaries: Implementations Extend Base" applies_to="**/*.ts">
These rules are ACTIVE for all TypeScript files, particularly:
- MCP server implementations (live-view-server, mcp-session, etc.)
- Proxy server implementations for replay and debugging functionality
- CLI utility servers that expose functionality to external clients
- Browser package integration servers that bridge web and native contexts
- Session management components that maintain stateful connections

**Out of scope:**
- Internal module-to-module function calls within the same service boundary
- Direct database access patterns
- UI component communication patterns
- Build-time code generation and compilation processes

**Exceptions:**
- EX-001: Legacy integrations that predate this ADR and require significant refactoring
- EX-002: Prototype or experimental features in isolated feature branches
</rule_activation>

## Context

The system requires integration between multiple services including CLI utilities, browser packages, and MCP (Model Context Protocol) servers. Service boundaries need clear definition to enable independent development, testing, and deployment of components. The pattern signature appears consistently across 3 files with 91.17% confidence, indicating a stable architectural pattern.

## Rules

**Note:** The source ADR does not define explicit R-PREFIX-### rule identifiers. The following rule is extracted from the Policy Block:

- **MAY**: Implementations MAY extend base server patterns with domain-specific functionality while maintaining protocol compliance

## Implementation Guidance

When implementing service integration boundaries:

- Use TypeScript interfaces to define server contracts and ensure type safety across integration boundaries
- Implement structured logging at server boundaries to enable distributed tracing and debugging of integration issues
- Consider using a base server class or mixin pattern to share common lifecycle management, error handling, and logging logic
- Document MCP tool registrations and protocol extensions in a central registry to prevent naming conflicts and enable discovery
- Implement graceful shutdown handlers that drain in-flight requests and clean up resources before process termination

## Verify

```bash
# Check for server implementation files matching the pattern
grep -r "class.*Server" --include="*.ts" | grep -E "(MCP|Proxy|Session)" | wc -l

# Check for server interface implementations
grep -r "implements.*Server" --include="*.ts" | wc -l

# Check for lifecycle methods in server files
find . -name "*-server.ts" -o -name "*-session.ts" | xargs grep -l "start\|stop" | wc -l
```

**Accept when:**
- At least 3 server implementation files exist matching the pattern (*-server.ts, *-session.ts)
- Server classes implement lifecycle methods (start, stop) for operational management
- MCP protocol compliance is verified through protocol-specific tests or validation

<enforcement>
**Verification Requirements:**
- Automated code review checks for server class naming conventions and interface compliance
- Integration tests that verify protocol compliance and lifecycle management
- Architecture review for new service integration points

**Violation Handling:**
- CI pipeline fails if server implementations lack required lifecycle methods
- Code review blocks merge if integration boundaries bypass server pattern without documented exception
- Quarterly architecture audits identify non-compliant integrations for remediation

**Exception Process:**
- Submit exception request to architecture review board with justification and impact analysis
- Provide migration plan with timeline if exception is temporary
- Document approved exceptions in service README and architecture decision log

Claude Code MUST NOT skip or defer verification when working with service integration boundaries.
</enforcement>

## Risks and Mitigations

- **MCP protocol evolution**: Version MCP server interfaces, implement protocol version negotiation, maintain backward compatibility layers
- **Server process failures**: Implement health checks, timeout policies, retry logic with exponential backoff, circuit breaker patterns
- **Session state management**: Implement session timeout policies, connection pooling with limits, automated cleanup of idle sessions